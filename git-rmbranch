list=$(git branch --merged |\grep -vE "release|master|develop|\*")
for B in $list; do
#  echo "$B\t <- Remove this branch? [y/n]"
#  read ans
#  if [ "$ans" = "y" ]; then
#    git branch -D $B
#    echo "Remove $b branch."
#  else
#    echo "Not remove."
#  fi
  git branch -d $B
  echo "Remove $b branch."
done
# TODO: remote -> git push --delete origin feature...
# TODO: -D/-d option
# TODO: unmerged message 

